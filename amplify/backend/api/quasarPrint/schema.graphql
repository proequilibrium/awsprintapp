type Todo @model @searchable @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  owner: String
  date: AWSDate
  description: String
  format: String
  completed: Boolean!
  pdf: S3Object
}

input S3ObjectInput {
  bucket: String!
  region: String!
  key: String!
}